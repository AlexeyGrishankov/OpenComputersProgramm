local impact_lsc = {}

-- Get all info. Stored, Capacity, Input, Output
function impact_lsc.getStoredLSC()
    -- return array
end

-- Get capacity percent
function impact_lsc.getChargePercent()
    -- return number
end

-- Get selected stored percent, stored < per = -1, stored == per = 0, stored > per = 1
function impact_lsc.getSelectChargePercent(per)
    -- per:number

    -- return number
end

return impact_lsc
